package dev.mariorobert.module1

/**
 * Step 1 :
 * Écrivez une "classe" Logger qui permet de stocker des logs (des String).
 * Logger doit avoir deux fonctions :
 * - addLog(message: String) : ajoute un log (mais ne print rien)
 * - printLogs() : print tous les logs
 *
 * Logger doit stocker les logs en interne.
 *
 * On veut pouvoir utiliser Logger sans avoir à l'instancier.
 *
 * Utiliser le String Template pour formater les logs.
 * Exemple : logs = "$logs$message\n"
 *
 * Step 2 :
 * Utilisez Logger dans votre [MessageValidator] de l'exercice précédent pour logguer chaque validation.
 * Logguez le message à valider, le mode (strict ou non) et le résultat de la validation.
 */
fun main(args: Array<String>) {
}
